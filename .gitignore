# ── Claude Code дотоод файл ───────────────────────────────
.claude/

# ── Хамаарал (npm install-аар сэргээгдэнэ) ───────────────
node_modules/
client/node_modules/
server/node_modules/

# ── Нууц мэдээлэл (.env.example файлууд git-д байх ёстой) ─
.env
server/.env
client/.env
# .env.example файлуудыг ignore хийхгүй — template учраас

# ── Build / compile output ────────────────────────────────
# Electron packaged app
client/dist/
client/out/
# TypeScript compile output (цаашид server TypeScript болбол)
server/dist/

# ── Log файлууд ───────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS тусгай файлууд ────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE тохиргоо (хувийн, repo-д оруулах шаардлагагүй) ───
.vscode/
.idea/
*.swp
*.swo

# ── Зураг файлууд (root хавтасны screenshot-ууд) ─────────
# client/src/renderer/icon.png зэрэг кодын зургийг ХАСАХГҮЙ
/*.png
/*.jpg
/*.jpeg
/*.docx

# ── WC3 replay файлууд (хэрэглэгчийн орон нутгийн файл) ──
*.w3g
resources/ZeroTierOne.msi

# Relay capture жишээ (бодит тоглоомын урсгал — git-д оруулахгүй)
hostbot/captures-sample/
